echo "Hello World"

entry=~/bin/processing-1.5.1

function idempotentAddToPath() {
    if ! grep -q $entry <<<$PATH
  then
    echo "not present"
  else
    echo "already present"
  fi
}

idempotentAddToPath $entry
